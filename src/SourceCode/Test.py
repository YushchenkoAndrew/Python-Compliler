def main():
    test = "Hello"
    a = -1
    b = not not 10 + -----1.5
    a = 1 + 25 / 6.5 + 2.9 * 2.5 + 2.5 * 2
    # a = not not 5 + 4
    return a or 1.5

    # Bug with this
    # return 1.5 + 5.2

main()