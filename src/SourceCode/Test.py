def main():
    # test = "a = "
    # a = ~1 + 2 + 3
    b = 5 + ~8 + 3 * 2
    c = not b + 2 * 4 + 5 * 1
    d = c
    # test = -10
    # a = 1 + ~2 + 3
    # a = 1 + 2 + ~3
    # a = 1 + ~ 1 and 2
    # a = not 1 + 5 * 2 * 3 * 1
    # a = not ~~~5 * 2 + 1
    # a = not not 1 + 2
    # a = 3 + 5 + ---2 * 4 + 1
    # a = -~8
    # return not test

main()